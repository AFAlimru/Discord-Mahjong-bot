# 更新紀錄（Changelog）

版本格式為 `大版本.小更新.熱更新`（MAJOR.MINOR.PATCH）：
大版本＝重大改動、小更新＝新增功能、熱更新＝一兩個小功能或修 bug。

## [未發布]

### 新增
- 大廳面板新增 **🔁 更新面板**（管理員）：把面板刷成最新版（機器人改版後新按鈕才會出現），
  不必刪頻道重建；順便切成點按者的語言

### 修正
- 和牌牌型（`/mahjong profile` 最高和了、和牌儀式、結果、回放）的空白整理：牌之間相鄰，
  只在**手牌↔副露、副露↔副露、`｜` 之前**各留一個半形空白；副露內部（含暗槓）不留空。
  `emojify_hand` 於渲染時處理，**舊紀錄也一併變整齊**；句子動態裡單張牌前後的空格不受影響。

## [0.6.3] - 2026-07-13

### 新增
- **排隊通知訂閱**：`/mahjong notify` 或大廳 🔔 按鈕開關——有人加入配對排隊時**私訊通知你**
  （含目前等待人數，每人最多 10 分鐘一次；本人／對局中／已在排隊者不打擾）
- **指定遊玩頻道 `/setup channel`**（管理員）：限制 `/mahjong start`・`/join` 只能在指定頻道使用
  （`clear:True` 解除）；其他頻道使用會提示到哪裡玩
- **加入伺服器自動建「設定指南」頻道**：僅管理員可見，含快速設定步驟（/setup create → /setup channel）、
  玩法一覽、權限需求、**刪除指南頻道**按鈕與**支援伺服器**連結；依伺服器地區語言顯示（中/日/英）

- 網站新增**服務條款 `/terms` 與隱私權政策 `/privacy`** 頁面（頁尾連結；供 Discord 開發者後台填寫）
- `/setup create` 權限降級：機器人權限不足時改「先建頻道再鎖權限」，鎖不了會提示手動；錯誤附原因

### 變更
- **大廳訊息不再殘留**：湊滿開局或 `/mahjong end` 關房時，「麻將房間開啟！」那則大廳訊息**直接刪除**
  （原本只是改字＋按鈕反灰留在頻道）
- **牌河訊息版面**：副露從名字旁移到**自成一行**（名字 → 副露 → 牌河）

## [0.6.2] - 2026-07-09

### 新增
- **`/setup create` 遊戲大廳頻道**（需管理頻道權限）：建立一個**唯讀頻道**（不會洗頻），
  只放一則常駐按鈕面板——🏅 段位賽／🎲 休閒場（點了選四人或三人直接排隊）、✅ 每日簽到、
  👤 個人資訊、🎒 倉庫（牌風切換＋解鎖狀態）、📜 牌譜、🎞 回放（輸入房號）、📖 役種表、
  🗣 語言、📢 公告（最新更新日誌）、🌐 網頁。按鈕為 persistent view，機器人重啟後照樣有效。
  面板文字與按鈕標籤**依建立者的語言**生成（日文管理員建的就是日文大廳）；
  各按鈕的回覆一律依「點按者」自己的語言顯示。
- **隨機匹配 `/mahjong match`**：跨伺服器湊人、對局走私訊、**不計段位**（休閒）；在**私訊裡打 `/mahjong join`** 也會進隨機匹配
- **開局對戰表**：段位賽／隨機匹配／打電腦開始時，先私訊一張對戰表（各家風位・名字・段位）
- **排隊逾時分級**：休閒匹配 **5 分鐘**、段位賽 10 分鐘沒湊滿自動退出並私訊通知
  （就算把排隊的提示訊息關掉，也不會卡在佇列裡）

### 新增（在地化）
- **斜線指令描述全面在地化**：指令／參數／選項的說明會依使用者的 **Discord 介面語言**顯示
  （日文介面看到日文說明、英文介面看到英文；`CommandTranslator`＋`cmdtr.*` 翻譯鍵）。指令名稱維持英文。
- `/help` 使用說明補上 `/setup create`（三語）。

### 變更
- **段位賽同段匹配**：只與段位相近者配對——同一場的最高與最低段位差不超過 2（＝各人上下一段）
- **對戰表標題含場別＋規格**：段位賽＝「🏅 段位賽 四人」（顯示人數）；休閒場＝「🎲 休閒場 四人南」（人數＋戰長，東＝東風／南＝半莊）
- **私訊對局改用名字、不再用 @ 提及**（DM 裡看不到別人的 @；輪到誰、下家是誰都直接顯示名字）

## [0.6.1] - 2026-07-08

### 新增
- **`/help` 改為使用說明**：指令一覽、如何開房／DM 打電腦／段位賽（三語）；移除重複的
  `/mahjong help`（出牌輸入說明仍在對局面板的「❓ 說明」按鈕）
- **`/mahjong yaku` 役種一覽**：飜數表（1/2/3/6 飜、役滿、雙倍役滿、懸賞），依語言顯示，含房規註記
- 後台可**用 Discord ID 直接查玩家**：簽到／活躍度、四人＋三人的段位與戰績、以及該玩家與機器人的共同伺服器
- 後台新增「**玩過的人**」清單（有對局或簽到過的都列出 ID，可點 ID 直接查）
- 個人紀錄頁補上**段位／R 值**

- 振聽／無役警示會**點名無役的牌**——「⚠️ 🀇 無役（這些牌和不了）」

### 修正
- **被碰／吃／槓走的捨牌沒算振聽**（此問題自早期版本即存在）：振聽判定改用完整捨牌史，牌河顯示不變
- **和牌／流局後舊牌桌殘留在討論串**：一局結束立即清掉該局的牌河與手牌面板，下一局重發

## [0.6.0] - 2026-07-07

詳細內容見 [CHANGELOG_DETAILS.md](CHANGELOG_DETAILS.md)。

### 新增
- 牌面全面改用**自訂表情**（38 張自製牌圖、全伺服器通用），Unicode 全替換，回放／舊牌譜相容
- **按鈕出牌**（依萬條餅字分列）＋**立直宣言按鈕化**（可宣言的牌亮綠、其餘鎖定）
- **黑色牌風**：段位達「日全食」解鎖，`/mahjong skin` 切換（只影響自己的畫面）
- **段位改名（月蝕主題）**：新人→新月→殘月→上弦月→下弦月→月偏食→日全食→血月
- **DM 跟電腦打**：私訊 `/mahjong start` 直接 AI 補滿開打（不列段位）
- 規則：**食替禁止**（預設、可房間開關）；**開立直**（房間開關、2 飜＋亮牌，放銃＝役滿、立直家豁免）
- 拔北鈕（常駐整回合）、**振聽／無役警示**、鳴牌後打出合併成一句動態
- 私人畫面拆成兩則訊息：**場況＋完整牌河**／**手牌面板**（含立直棒顯示）

### 變更
- 副露只顯示 `[牌]`、動態行放大、換局自動清掉上一局訊息、移除 `/mahjong watch`

## [0.5.0] - 2026-06-29

### 新增
- **`/mahjong replay <房號>` 牌譜回放**：在頻道開一個公開討論串，**像對局中那樣逐格「播放」**該場
  對局——更新同一則牌桌訊息，各家**手牌／牌河／副露／動作**依原順序一步步推進，和牌時顯示和了牌型。
  - **顯示每位玩家的手牌**：牌譜逐步記錄各家當下手牌（`move` 事件加 `hands`），回放精確重現。
  - **播放速度依當時實際出牌間隔**（牌譜時間戳，夾在 0.4–8 秒）。
  - 畫面做成**像對局中的牌桌**：局/巡、各家風位/名字/副露/牌河、▶ 標出視角玩家，下方放大顯示該玩家手牌。
  - **和牌時重現和牌儀式**：逐項揭曉役種，最後公布飜符／等級／點數，並顯示寶牌／裏寶牌
    （結算事件新記 `wd`／`dora`／`ura`，僅新局有效）。
  - **控制按鈕**：◀局／◀巡／◀步／步▶／巡▶／局▶ 導覽、⏸ 暫停／▶ 繼續（自動播放）、
    **👁 切換視角**（輪流看每家手牌）、**✖ 退出**（刪掉回放討論串）。
  - 由牌譜重建（`mahjong/replay.py`）；限 0.4.3 起有牌譜的對局，手牌顯示僅對加上 `hands` 之後的新局有效。

### 修正
- `/mahjong history` 在只有一頁時傳入 `view=None` 造成例外，改為單頁不帶 view。
- **`/mahjong history` 牌譜列表（分頁）**：用 ◀ ▶ 按鈕翻頁看自己的對局（含**房號**、順位、終局、得失）。
- **網頁個人紀錄牌譜表加「房號」欄**（`get_recent_records` JOIN `games.room_no`）；`db.get_game_by_room_no` 供回放以房號查對局。
- **`/mahjong profile` 個人資訊卡**：頭像、ID、帳號創建時間、段位／R、活躍度／連續簽到、對局場次，
  以及**最高和了**（最大和牌打點＋等級，如役滿，並附上當時的**和牌牌型** Unicode 牌字串）。最高和了於每場對局結束時自動更新。
- **任務／活躍度系統**：`/mahjong daily` 每日簽到（連續加成）、`/mahjong tasks` 查看今日任務與活躍度；
  每日完成一場對局自動發活躍度。資料存於 `user_activity` 表。
- **`/mahjong rankinfo`**：段位／R 與段位賽說明（與遊玩說明 `/help` 分開）。
- **段位賽：跨伺服器全域匹配（對局走 DM）**：
  - `/mahjong rank [sanma]`：加入全域排隊（跨所有伺服器共用一池），湊滿 4（三麻 3）人自動開局；附「離開排隊」按鈕。
  - 對局在每位玩家**與機器人的私訊**進行（手牌面板即含牌桌／牌河／動態，無公開串），故不需同伺服器。
  - 段位賽結束自動更新段位／R 並私訊變化；段位賽不補 AI、全真人。
  - **排隊逾時**：等待逾 10 分鐘仍未湊滿，背景清掃器會自動把你移出排隊並私訊通知。
  - `match_loop_t` 改為支援「無公開串／無頻道」模式以重用對局引擎；重啟時 DM 段位局直接結束（無法跨伺服器重建）。
- **`/mahjong stats` 顯示段位／R**（有段位賽紀錄時）。
- **網頁段位排行榜**（私有站，`/leaderboard`）：四人／三人分頁，依段位階級→段位點→R 排序。
- **網頁活躍度排行榜**（私有站，`/leaderboard?board=activity`）：與段位榜以切換鈕並列，依活躍度→連續簽到排序。
- **段位／R 計算引擎與資料表（0.5 地基）**：
  - `mahjong/rating.py`：天鳳風段位階梯（新人→級位→段位→天鳳位，依順位加減段位點、可升降段）
    與 R 值（依順位 + 對手平均 R 加權、對局數越多變動越小）。常數集中、易調整。
  - `user_rating` 資料表（分三麻／四麻），`db.apply_ranked_game()` 段位賽結束後更新真人段位／R
    （bot 不計入但納入對手平均），`db.get_leaderboard()` 排行榜查詢。
  - 尚未接到對局流程（段位賽開關、`/stats` 顯示、排行榜頁為後續工作）。

## [0.4.5] - 2026-06-26

### 修正
- **`/mahjong end` 全面修正**：
  - 等人中（大廳）也能正確關閉：大廳訊息改為「🚪 房間已關閉。」（原本不會處理、訊息也誤顯示為「牌局已強制結束」）。
  - 結束時會在**主頻道公告**與**每個討論串**貼出「🚪 房間已關閉。」，稍候再刪除。
  - **DB 立即標記為 `finished`**：原本沒標記，導致機器人重啟時把已結束的對局誤判為「中斷」而跳出回復提示。
  - 回覆文案統一為「🚪 房間已關閉。」。

## [0.4.4] - 2026-06-26

### 修正
- **`/mahjong end` 刪不掉討論串時不再無聲失敗**：原本 `except: pass` 把錯誤吃掉；
  現在會印出實際例外、並用訊息提示房主「請確認機器人有『管理討論串』權限」（多半是缺權限）。

### 變更
- **`/mahjong stats` 改為公開顯示**（原本只有自己看得到），並大幅美化：
  - 加上玩家頭像縮圖、順位獎牌分布。
  - 新增**頭名率／連對率／墊底率**（由戰績）。
  - 新增**和了率／放銃率／立直率／副露率／平均和了打點**（取自有牌譜的對局，0.4.3 起累積）。
  - 各語言（繁中／日文／英文）皆補上對應標籤。

### 新增
- 結算牌譜（settle 事件）加記**副露者**與**和了打點**，並新增 `db.get_hand_rates()`
  供 `/stats` 與網頁個人頁計算和了率／放銃率／副露率／平均和了打點。

## [0.4.3] - 2026-06-25

### 新增
- **牌譜結算落地 + `/mahjong repair`（戰績自動修復，限管理員）**：
  - 每局結算與開局名單現在會寫進 `game_log`（以 `user_id` 記錄勝負／放銃／立直／各家點數變動）。
  - 並逐手把公開動作（摸打／吃碰槓／立直／和牌等）以 `move` 事件落地，為日後的牌譜重播鋪路。
  - `/mahjong repair` 掃描 `game_records`：去除重複紀錄、依結算牌譜**重算**自摸／榮和／放銃／
    放銃失點／獲得點數／立直次數、負值歸零，並回報修復筆數。
  - 限制：**過去**沒有牌譜的對局無法回填逐手資料（立直等只能從今以後的對局正確累計）。

### 修正
- **立直次數統計恆為 0**：原本在 `advance_*`（已清除 `riichi` 旗標）之後才計算，導致每局都數到 0；
  改為在推進場次前統計，立直（含立直和牌）才會正確累計到 `/stats`。

### 變更
- **私人手牌面板版面調整**：
  - 場況／寶牌（局數・本場・牌山・寶牌指示）移到面板**最上面**。
  - 各家牌河的玩家行**放大**（含立直標記與副露）。
  - 去掉「你的手牌」標題（立直時仍會標註）；手牌與剛摸到的牌維持以 `|` 分隔。
  - 和牌手牌的和牌張由 `🟦[牌]` 改為以 `| 牌` 標示。

## [0.4.2] - 2026-06-25

### 新增
- **其餘途中流局**（接續 0.4.1 的九種九牌，補齊四種）：
  - **四風連打**：第一巡四人捨出同一張風牌、其間無人鳴牌即流局。
  - **四家立直**：四家全部宣告立直，且第四個立直宣言牌無人榮和即流局。
  - **四槓散了**：場上累計四槓且由兩人以上宣告即流局（同一人四槓＝四槓子，不流局）。
  - **三家和**：同一張捨牌同時被三家榮和即流局。
  - 以上一律莊家連莊、本場 +1、不移動點數，立直棒留到下一局；各語言（繁中／日文／英文）皆有對應流局標題與動態訊息。
- **雙榮（ダブロン）**：同一張捨牌被兩家同時榮和時，兩家**都**和牌，放銃者分別支付兩份，
  公開串與各私人串會依序揭曉兩位贏家的役種（最後才公布合計分數表）。
  本場棒（300×本場）與桌面立直棒一律歸「頭跳側」（離捨牌者最近的下家）；
  莊家若為其中一位贏家則連莊。三家同時和則改判三家和流局。

### 變更
- 途中流局統一為 `("abort", <result_key>)` 結算路徑（九種九牌一併沿用），方便日後擴充。
- **多家榮和由「頭跳」改為「雙榮」**：兩家同時聽同一張時不再只賠一家；
  並修正同優先序的選人——改為取離捨牌者最近的下家（原本固定挑最小座號）。

## [0.4.1] - 2026-06-24

### 新增
- **英文介面（`mahjong/language/en.json`）**：第三個語言。`/language` 現可選
  繁體中文／English／日本語。涵蓋所有 UI 標籤、按鈕、動態訊息、進聽提示、
  錯誤／提示 toast，以及 42 役名稱（Riichi／Pinfu／Tanyao…）、滿貫／役滿等級、
  寶牌等的英文標準術語；缺鍵仍自動退回繁中母本。
- **九種九牌（途中流局）**：自己第一巡、無人鳴牌、且 14 張含 9 種以上么九牌時，
  面板出現「🀫 流局（九種九牌）」按鈕；宣告後莊家連莊、本場 +1、不移動點數。

### 變更
- **公開內容跟隨房主語言**：大廳公告與觀戰牌桌改以「開房者設定的語言」顯示
  （房間設定新增 `lang` 欄位＝房主語言）。
- **「🌐 翻譯」按鈕**改為只列出「當前顯示語言以外」的語言（避免叫人翻成同一種）：
  繁中房→「🌐 Translate / 翻訳」、日文房→「🌐 翻譯 / Translate」、英文房→「🌐 翻譯 / 翻訳」；
  點擊仍以點擊者語言 ephemeral 檢視。

### 修正
- 私人手牌討論串名稱「{名} 的手牌」與「輪到你了！」提醒，改依玩家語言顯示（原本固定繁中）。
- 出牌輸入支援日文 `リーチ`／`ツモ`（含 `リーチ1s` 無空格）與 `北抜き`，不再被當成無效輸入。
- 出牌輸入錯誤的提示由「看不懂…」改為「沒有這張牌…」（多半是打了手上沒有的牌，語意更貼切）。
- 進聽提示的「榮和無役」標籤簡化為「無役」（繁中）。
- **`/mahjong stats` 改讀 `game_records`**：原本讀已停用的 `stats` 表（不再寫入）導致顯示空白；
  現改用每場戰績，分三麻／四麻顯示場次、平均順位、順位分布、自摸／榮和／放銃、立直、獲得點數、放銃失點。

## [0.4.0] - 2026-06-22

### 重點：多語系（繁體中文／日文）

新增完整的多語系系統。玩家用 `/language` 設定個人語言，存入資料庫永久記憶。
**只有自己看得到的內容**（私人手牌面板、出牌提示、各種按鈕、和牌儀式、最終順位、
進聽提示、錯誤訊息…）會以該玩家設定的語言顯示；**公開的共享訊息**（大廳公告、
觀戰牌桌）維持母本，並附「🌐 翻譯」按鈕，點了用自己的語言、僅自己可見地檢視。
要新增語言，只需在 `mahjong/language/` 放一個 `<code>.json` 照著翻（缺鍵自動退回
繁中母本），不必改任何程式。

### 新增
- **`/language` 指令**：僅自己可見的語言選單，選定即寫入 `user_prefs` 表（每人一筆、跨重啟記憶）。
- **`mahjong/language/` 語言檔**：`zh_tw.json`（繁中母本）、`ja.json`（日文）。涵蓋 42 役名稱、
  滿貫／役滿等級、寶牌、模式／戰長／風位、所有 UI 標籤與按鈕、動態訊息、進聽提示、
  和牌／流局／順位文字，以及全部錯誤／提示 toast。
- **`mahjong/i18n.py`**：`t(key, lang, **kw)` 取翻譯（缺鍵退回母本）、`yaku()` 譯役名、
  `get_user_lang`/`set_user_lang`（記憶體快取 + DB）、`available()`/`lang_name()`。
- **🌐 翻譯按鈕**：觀戰公開牌桌與大廳公告，皆可用點擊者語言 ephemeral 檢視內容。
- **結構化動態（action feed）**：每筆動作改以「翻譯鍵 + 參數」記錄，逐位玩家以各自語言
  即時渲染（出牌／摸切手切／吃碰槓／拔北／立直／輪到誰…）。
- **`DEV_GUILD_ID` 環境變數**：設定後斜線指令只同步到該測試伺服器，**即時生效**
  （免等全域指令約 1 小時的傳播延遲）；正式機留空走全域同步。
- **結算順位 Emoji**：每局點數變動的每一行標註該玩家當下名次（🥇🥈🥉/4️⃣）。

### 變更
- **天鳳式順位馬依戰長修正**：天鳳式四麻**東風戰**改為 `+10/+5/-5/-10`、**半莊**為
  `+20/+10/-10/-20`（原本不分戰長一律用半莊的 20-10）；三麻維持 `+20/0/-20`、返點 40000。
  預設（mixed）規則四麻仍一律 20-10。`final_standings` 新增 `length`/`ruleset` 參數。
  （雙倍役滿→單倍的天鳳判定本就正確：四暗刻單騎等算單倍，不同役滿仍可複合。）
- **大廳不再因等待逾時自動關房**（`LobbyView` 改 `timeout=None`）。等多久都不會自己關，
  只有關閉機器人（重啟後大廳失效）才結束；房主可用 `/mahjong end` 手動關閉閒置房。
- **加入別人的房間時，自動關掉自己開的、仍在等待中的房間**（一人同時只佔一房；
  已開始的對局不受影響），原大廳訊息標示已關閉。
- **合併 `client.py` 進 `run.py`**（單一啟動入口）：`bot`/`tree`、`on_message`/`on_ready`、
  `run()` 全移入 `run.py`；`commands.py` 改提供 `register(tree)` 由入口呼叫。
  啟動指令維持 `python run.py`（部署不受影響）。
- **開房設定面板**（數值／三麻／戰長／擊飛／規則／確認、數值設定對話框）依房主語言顯示。

### 修正
- **三麻全員聽牌流局 `ZeroDivisionError`**：聽牌罰符的守衛原本硬寫 `1<=n<=3`（假設四麻），
  三麻三人全聽時未聽者為 0 導致 `3000 // 0` 崩潰，整個對局迴圈中斷。改為
  `0 < 聽牌數 < 玩家數`，同時涵蓋三麻／四麻的「全聽／全不聽」。
- **`TextInput.label` 棄用警告**：數值設定對話框原本在 `__init__` 事後設定 `.label`
  觸發 discord.py 的 `DeprecationWarning`，改為在 `__init__` 直接以翻譯後的 label 建立欄位。

## [0.3.1] - 2026-06-18

### 新增
- **牌河移到手牌面板最上方**：往上滑即可看到各家牌河（放大顯示，`▶` 標目前出牌者、立直者標【立直】），不必再點按鈕。

### 變更
- 私人手牌串中，非自己回合打字會直接刪除（不殘留、也不給提示）。

### 修正
- 門清未立直、且待牌榮和皆無役（自摸有門前清自摸）時，進聽提示會標「榮和無役」並提醒須立直或自摸才能和（原本門清手永不標無役，會漏掉此情況）。

## [0.3.0] - 2026-06-14

### 重點：模組化重構、房間編號、重連回復

本版把專案從單一巨型 `Bot.py` 重整為 `mahjong/` 套件，並補上多項對局體驗與穩定性的改進。

### 新增
- **房間編號**：每間房分配「房間#0001」流水號，顯示於開房公告與討論串名稱；存入資料庫，重啟後續號不重複。
- **`run.py` 啟動入口**（取代直接執行 `Bot.py`）。
- **重連回復**：機器人重新啟動時，尚未結算的對局會在原頻道跳出「繼續／結束」按鈕——可從目前比分接續（重建討論串、續打下一局），或直接結算並公布最終順位，不再卡死。
- **詳細戰績紀錄**：每場對局結束後，記錄每位玩家的順位、終局點數、得失點、自摸／榮和／放銃次數、放銃失點、獲得點數與立直次數（分三麻／四麻）。
- **流局滿貫（流し満貫）**：捨牌全為么九且未被鳴走時以滿貫計分，並比照和牌畫面呈現。
- **ALL LAST 提示**：最終局開打前顯示「ALL LAST」橫幅（四麻東風＝東三局後、三麻＝東二局後，半莊則為南場對應局）；公開串與各私人手牌串同步顯示。
- **「摸切」按鈕**：一鍵丟掉剛摸到的牌（位於「說明」右側）。
- 和牌時**揭曉寶牌／裏寶牌指示牌**，列於手牌上方，格式 `[寶牌：🀉 🀫 🀫 🀫 🀫]`（立直才顯示裏寶牌）。

### 變更
- **模組化重構**：將單一 `Bot.py`（約 3357 行）拆成 `mahjong/` 套件——`client`／`commands`／`flow`／`ui`／`render`／`rules`／`engine`／`scoring`／`settlement`／`views`／`db`／`config`／`state`／`rooms`。
- **最終順位**同時貼到公開串與每位玩家的私人手牌串（不再只在公開串），確保各自的討論串都看得到結束結果。
- 牌桌與手牌面板的寶牌指示不再標「寶牌」字樣（只留放大的指示牌）。
- `.env.example` 重寫，每個變數附說明；README 更新檔案結構，安裝／啟動改為**虛擬環境**流程。

### 修正
- **流局聽牌**改用含副露的判定：原本只看門前手牌，吃／碰／槓後手牌變短會被誤判未聽，導致該收的聽牌料沒算到——現已修正（摸切後的「聽牌！」提示同步修正）。

### 移除
- 刪除 0.1 舊版死碼（約 1018 行）：`TurnView`、舊 `play_hand`/`match_loop`、舊 DM 版反應、未使用的工具函式，以及舊版動作指令 `/ron` `/pon` `/chi` `/kan` `/tsumo` `/riichi` `/skip` `/kita`（0.2 起改用討論串按鈕＋打字，這些已無作用）。

## [0.2.2] - 2026-06-12

### 新增
- **加槓（小明槓）與搶槓**：補上 0.2 討論串流程缺少的加槓按鈕與搶槓窗口。
- **玩家動態**：私人手牌面板上方顯示最新一筆動作（出牌手切／摸切、吃碰槓、立直、自摸、拔北、輪到誰），完整動態用「📜 看動態」按鈕查看。
- **進聽提示標註**：每個可進聽打法後標註「（無役）」（湊不出役、不可和）或「（振聽）」（只能自摸），僅在實際發生時顯示。

### 變更
- **立直後鎖手**：面板顯示「【已立直】」，只能摸切或自摸，不可換牌、不可副露（碰／吃／槓）。
- **和牌儀式分階段揭曉**：先放「榮和／自摸」標題，再放手牌，接著逐一揭曉役種與點數；公開串與各私人串同步播放，結束後保留完整結果並倒數 5 秒自動進入下一局。
- **觀戰結束自動清理**：觀戰對局結束約一分鐘後自動刪除討論串。
- **手牌面板版面**：場況／寶牌改為引言、上下加分隔線；「上家打出」與「最新動作」位置對調；出牌提示精簡，細節導向「說明」。

### 修正
- 倒數時保留完整和牌結果，不再縮成只剩放銃標題。

## [0.2.1] - 2026-06-12

### 修正
- 聽牌判定（`is_tenpai` / `get_tenpai_waits`）原本會跳過「手中已持有的待牌」，導致**雙碰、多面聽**（例如摸切白後雙碰聽 2s/8s）被誤判為未聽牌，因而無法立直或和牌。
- 同一處還無條件跳過 **2-8 萬** 的待牌，四麻時這些牌的聽牌會被漏判（例如 34 萬的兩面聽）。
- 改為一律用 `is_complete` 逐張試聽，與進聽提示 `tenpai_advice` 的判斷一致。

## [0.2.0] - 2026-06-12

### 重點：討論串 UI 大改版
牌桌由單一頻道訊息搬到 **Discord 討論串**：大家共用的公開討論串（聊天／結果，觀戰模式則顯示牌桌），每位玩家各自一條私人討論串顯示自己的手牌，敏感資訊不再外洩。

### 新增
- **私人手牌面板**：即時倒數、場況（局／本場／牌山）、寶牌、剛摸到的牌以 `｜` 分隔、副露、上一張被打出的牌、進聽提示（打哪張可聽哪些）。
- **打字出牌**：在自己的私人討論串直接輸入牌（`5m`、`東`、紅寶牌 `0s` 等），打完自動刪字。
- **行動按鈕**：自摸／立直／暗槓／拔北（三麻），以及吃／碰／槓／榮和／跳過，皆附倒數計時。
- **立直按鈕可切換**：宣告後再按一次即取消，回到一般出牌。
- **資訊按鈕**：看牌河／看點數／看副露／說明，輪到自己時也能查看。
- 回合 **@ 提醒**（出牌後自動刪除）、**拔北確認按鈕**。

### 變更
- 真人對局移除公開牌桌，改用按鈕查看資訊（避免洩漏聽牌）。
- 牌面、寶牌、進聽提示等改用 `#` 放大；牌桌名字以「」標註。
- 行動按鈕配色：吃＝綠、碰／槓＝藍、榮和／自摸＝紅、立直／跳過＝灰。
- 吃的候選牌組移出按鈕、改放大寫在訊息裡（多組以 ①②③ 標示）。
- 看點數只顯示各家點數；和牌／流局時先清除手牌面板，讓結果獨佔畫面。
- 起始點數自訂範圍改為 `0~1000000`。
- 全專案簡體字統一改為繁體中文。

### 修正
- 三麻活牌數錯誤（70→94），導致只打幾張就流局。
- 立直判定改為「門清且任一打能保持聽牌」，修正換張聽牌時無法宣告立直。
- 流局時 `header`／`hand_str` 未定義、和牌時 `tenpai` 未綁定的 `UnboundLocalError`。

## [0.1.1] - 2026-06-10

### 新增
- 出牌狀態顯示**摸切／手切**（打出剛摸的牌＝摸切，打出原本手裡的牌＝手切）。
- 新增本更新紀錄（CHANGELOG）。

### 變更
- 手牌牌名格式改為「花色字母在前」：`m123456 s123 p55 東中`。
- 吃的私訊提示改用 **Unicode 牌面並放大**（原本只顯示 `123s` 文字）。
- README：GPLv3 連結改為可點擊、加上 vibe coding 註記、補充需啟用 Message Content Intent 的說明。

### 修正
- 「看我的牌」在別人回合時點擊無回應（每局牌桌重建後視圖物件被重用所致；改為每局建立新視圖、回合視圖改長逾時並於結束後關閉）。
- 修正手牌牌名中**龍牌對應錯誤**（發／白顛倒）。

## [0.1.0] - 2026-06-10

### 首次公開發佈
- 完整日本麻將：四麻／三麻、東風戰／半莊戰。
- 42 役 + 七對子／國士無雙、副露減飜、無役不可和、符數與點數（標準精算）。
- 振聽、一發、兩立直、嶺上開花、海底／河底、槍槓、天和／地和。
- 三麻拔北、加槓／搶槓、暗槓／大明槓。
- 連莊本場、立直棒、流局聽牌罰符、擊飛、賽末順位馬點精算。
- AI 玩家、觀戰模式、公平性驗證（SHA-256 牌山編碼）。
- 單一面板訊息持續編輯、即時倒數、敏感資訊走私訊、和牌儀式。
- 授權：GPLv3。
